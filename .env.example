# Telegram Bot Configuration
BOT_TOKEN=your_bot_token_here

# Database Configuration
# Options: sqlite, postgresql
DATABASE_TYPE=sqlite
# For SQLite (default):
DATABASE_URL=sqlite+aiosqlite:///./bot.db
# For PostgreSQL (when using docker-compose):
# DATABASE_URL=postgresql+asyncpg://rodnulya:rodnulya_password@postgres:5432/rodnulya

# HTTP Server Configuration
HTTP_HOST=0.0.0.0
HTTP_PORT=8080

# Webhook Security
# Change this to a random secure string in production
WEBHOOK_SECRET=change_me_in_production
